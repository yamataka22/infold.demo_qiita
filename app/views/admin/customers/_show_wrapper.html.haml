= modal.with_header do
  %ol.breadcrumb.text-dark
    %li.breadcrumb-item CUSTOMERS
    %li.breadcrumb-item.active= "#{t('infold.operation.show')} ##{customer.id}"
= modal.with_body do
  .modal-body
    = render 'show_content', customer: customer
- unless admin_remote_modal_id == 'modal_sub'
  = modal.with_footer do
    .modal-footer
      = link_to t('infold.operation.edit'), edit_admin_customer_path(customer),
        class: 'btn btn-primary', data: { turbo_frame: "modal_main" }
      = link_to t('infold.operation.delete'), admin_customer_path(customer),
        class: 'btn btn-danger ms-1', data: { turbo_confirm: t('infold.operation.confirm',
        submit: t('infold.operation.delete')), turbo_method: :delete, turbo_frame: "_top" }